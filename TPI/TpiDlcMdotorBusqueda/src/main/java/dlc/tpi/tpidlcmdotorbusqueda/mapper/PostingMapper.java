package dlc.tpi.tpidlcmdotorbusqueda.mapper;

import org.springframework.stereotype.Service;

@Service
public class PostingMapper {
/*
    public PostingSlot mapFromEntity(PosteoEntity posteoEntity){

        PostingSlot postingSlot = new PostingSlot();
        postingSlot.setDocumentUrl(posteoEntity.getDocumento().getUrl());
        postingSlot.setTokenFrecuency(posteoEntity.getTf());
        return postingSlot;

    }

    public List<PostingSlot> mapAllFromEntity(List<PosteoEntity> posteoEntityList){
        if(posteoEntityList != null){
           return posteoEntityList.stream().map((posteoEntity -> this.mapFromEntity(posteoEntity))).collect(Collectors.toList());
        }else{
            return null;
        }

    }*/

}
